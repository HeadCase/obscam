[Service]
# AllSky keeps normal service capacity while the latency-sensitive ObsCam path
# receives favorable contention weights and asiair-sync yields first.
CPUWeight=100
IOWeight=100
OOMScoreAdjust=-100
